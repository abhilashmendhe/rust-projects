Builder Design Pattern
----------------------
It is creational design pattern used to create complex presentation of objects. It makes flexible to build objects.
Rather than passing everything in the constructor parameters we can simply call the methods to create the object.



